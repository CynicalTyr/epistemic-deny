# Names only. This library does not read secrets at runtime.
# Copy to .env if your host dispatcher later uses a named demo dir.
# Never commit .env.
#
# Optional demo dir for *your* worker, not this kernel:
# AGENT_HOME=/tmp/epistemic-deny-demo
